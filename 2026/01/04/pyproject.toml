[project]
name = "04"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ontouchstart",
]

[tool.uv.sources]
ontouchstart = { git = "https://github.com/ontouchstart/ontouchstart.github.io.git", rev = "2026_01_04" }
