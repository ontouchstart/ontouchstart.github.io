[package]
name = "grrs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.53", features = ["derive"]}
find_matches = { git = "https://github.com/ontouchstart/ontouchstart.github.io.git", branch = "find_matches" }

