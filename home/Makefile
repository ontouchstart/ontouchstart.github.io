all:

# https://hub.docker.com/r/jsii/superchain
version:
	node --version
	yarn --version
	python3 --version
	aws --version
	sam --version
	docker --version
	make --version
	rsync --version
	gh --version

clean:
