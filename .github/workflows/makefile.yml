name: Makefile CI

on:
  push:
    branches: [ "2026_01_06_py" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - uses: astral-sh/setup-uv@v7
    - name: Make
      run: make
