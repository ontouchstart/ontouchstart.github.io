name: make

on: 
  push:
    paths-ignore:
      - 'ontouchstart.github.io'
      - '.github'

jobs:
  make:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: make
