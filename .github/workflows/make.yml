name: make

on: 
  push:
    paths-ignore:
      - 'ontouchstart.github.io'
      - '.github/workflows/*'

jobs:
  make:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: make
