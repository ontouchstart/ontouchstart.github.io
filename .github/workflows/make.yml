name: make

on: push

jobs:
  make:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: make version
        run: make  version
      - name: make 
        run: make 
