name: make

on: push

jobs:
  make:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: make build
        run: make build
      - name: make test
        run: make test
