[project]
name = "18"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipython>=9.8.0",
    "mlx-lm",
    "openai>=2.13.0",
    "ruff>=0.14.9",
]

[tool.uv.sources]
mlx-lm = { git = "https://github.com/ontouchstart/mlx-lm", rev = "main" }
openai = { git = "https://github.com/ontouchstart/openai-python" }
